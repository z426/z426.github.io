---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0.1" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>{{ site.title | xml_escape }}</title>
    <description>{{ site.description | xml_escape }}</description>
    <link>{{ site.url }}{{ site.baseurl }}/</link>
    <atom:link href="{{ "/feed.xml" | prepend: site.baseurl | prepend: site.url }}" rel="self" type="application/rss+xml"/>
    <pubDate>{{ site.time | date_to_rfc822 }}</pubDate>
    <lastBuildDate>{{ site.time | date_to_rfc822 }}</lastBuildDate>
    <generator>Jekyll v{{ jekyll.version }}</generator>
    {% assign posts = site.posts %}
    {% assign photos = site.photos | reverse %}
    {% assign feed = posts | concat: photos | sort | reverse %}

    {% for post in feed limit:10 %}
      <item>
        <title>{{ post.title | xml_escape }}</title>
        <description>{{ post.content | xml_escape | truncatewords:30 }}</description>
        <creator>{{ site.authors[post.author].display_name  }}</creator>
        <author>{{ site.authors[post.author].display_name  }}</author>
        <media:group>
          <media:content medium="image" url="{{ site.url}}{{ post.image}}" type="image/jpeg"/>
        </media:group>
        <pubDate>{{ post.date | date_to_rfc822 }}</pubDate>
        <media url="{{ site.url}}/{{ post.image}}" medium="image"></media>
        <link>{{ post.url | prepend: site.baseurl | prepend: site.url }}</link>
        <guid isPermaLink="true">{{ post.url | prepend: site.baseurl | prepend: site.url }}</guid>
        {% for tag in post.tags %}
        <category>{{ tag | xml_escape }}</category>
        {% endfor %}
        {% for cat in post.categories %}
        <category>{{ cat | xml_escape }}</category>
        {% endfor %}
      </item>
    {% endfor %}
  </channel>
</rss>
